[package]
name = "rttp_test_support"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
base64 = { version = "0.22", optional = true }
rcgen = { version = "0.11", optional = true }
rustls = { version = "0.23", optional = true }
socket2 = "0.5"

[features]
tls-rustls = ["dep:base64", "dep:rcgen", "dep:rustls"]
